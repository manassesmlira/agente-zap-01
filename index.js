// index.js
// Este arquivo serve como um ponto de entrada para o Vercel iniciar o projeto.
// Ele não fará nada por si só, mas permitirá que suas API Routes funcionem.

console.log("Projeto Vercel iniciado via index.js!");

// Para satisfazer o Vercel, exporte uma função vazia ou um objeto.
// Isso indica que o módulo foi carregado com sucesso e não há nada mais a ser feito.
export default () => {};

// Ou, se preferir exportar um objeto vazio:
// export default {};